#ifndef CAPTURE_H_
#define CAPTURE_H_

#include "../server/ships.h"
#include "../server/vars.h"

void capture(int, int, int, int, orbitinfo *);
void capture_stuff(shiptype *);

#endif /* CAPTURE_H_ */
